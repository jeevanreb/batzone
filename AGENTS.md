Instructions to any AI:
1. Every section must be divided in gsap and markup component as gsap is client side component. Also while creating markup component, use MaxWidth.
2. If the markup component has any event tag like button then it must be separated so that it won't effect the server side markup component.
3. Assets of every section is kept on assets folder with same section name.
4. For tailwind css value, try to use xs, sm, lg, xl, 2xl, etc. not custom px. Also try to use color value instead of custom color.
5. Follow the proper project folder and name convention pattern.
6. In gsap component, never use useRef. And only use useGsap not useEffect or useLayoutEffect.
7. Whenever adding any id to any tag, name it unique as per section name.
8. Whenever figma design or any screenshot image pasted to generate code, always create exact same as per that 100%.
9. Always create section with responsiveness to desktop, tab and mobile.